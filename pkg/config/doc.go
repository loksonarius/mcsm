// Package config contains the Definition of the general ConfigFile struct that
// should be used to translate from abstract configuration maps to concrete
// files on the filesystem. Besides providing the tools for defining these
// items, this package also includes predefined ConfigFile templates for common
// files such as the popular server.properties or the shared spigot.properties.
package config
